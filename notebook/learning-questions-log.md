# Transformer量化学习问题分类清单

## 使用说明
- 目标：把学习过程中的问题按专题归类，避免问题散落。
- 规则：每次出现新问题时，先归类再记录“现状-假设-验证计划”。
- 记录语言：中文。

## A. 任务定义与标签设计
- 典型问题：
  - 预测方向、收益还是波动率？
  - 预测 horizon 选 1/5/20 bar 的依据是什么？
  - 标签是否受极端行情污染？
- 记录模板：
  - 问题：
  - 当前假设：
  - 验证方法：
  - 结论与下一步：

## B. 数据与特征工程
- 典型问题：
  - OHLCV 与哪些指标组合最有效？
  - 指标窗口是否过长导致滞后？
  - 多资产数据如何对齐与补齐？
- 记录模板：
  - 问题：
  - 当前假设：
  - 验证方法：
  - 结论与下一步：

## C. Embedding层设计（重点）
- 典型问题：
  - 线性投影还是MLP投影更适合当前样本量？
  - 是否需要 `E_time`、`E_asset`、`E_pos` 同时存在？
  - 多时间尺度应在输入侧融合还是embedding后融合？
- 记录模板：
  - 问题：
  - 当前假设：
  - 验证方法（含消融）：
  - 结论与下一步：

## D. 注意力机制与信息泄漏
- 典型问题：
  - 双向注意力是否引入未来信息？
  - 因果mask是否在训练与推理完全一致？
  - rolling特征是否误用未来窗口？
- 记录模板：
  - 问题：
  - 潜在泄漏路径：
  - 防护方案：
  - 回测复核结果：

## E. 训练与泛化
- 典型问题：
  - 过拟合来自模型容量还是特征噪声？
  - 学习率、权重衰减、dropout 的敏感性如何？
  - 不同市场阶段表现是否稳定？
- 记录模板：
  - 问题：
  - 当前假设：
  - 验证方法：
  - 结论与下一步：

## F. 回测与交易可行性
- 典型问题：
  - 预测提升能否转化为策略收益？
  - 加入手续费、滑点后是否仍有优势？
  - 信号频率与换手是否可执行？
- 记录模板：
  - 问题：
  - 回测设定：
  - 关键指标：
  - 结论与下一步：

## G. 工程与复现
- 典型问题：
  - 数据版本、特征版本、模型版本如何绑定？
  - 同一实验是否可一键复现？
  - 结果记录是否支持横向比较？
- 记录模板：
  - 问题：
  - 复现路径：
  - 依赖与版本：
  - 结论与下一步：

## 当前优先问题池（持续更新）
- [ ] Embedding中 `E_time` 对短周期任务的边际贡献是多少？
- [ ] 指标特征是否在不同市场状态下存在漂移？
- [ ] 因果注意力与局部注意力结合能否提升长序列表现？
- [ ] 回测中的收益改善是否主要来自少数极端区间？

## 已决策的设计选择（C. Embedding层）
> 相关代码：`transformer_kit/embeddings.py`、`transformer_kit/features.py`
> 相关笔记：`notebook/bert-style-embedding-implementation.md`

- 问题：单资产场景下 `E_asset` 是禁用还是保留 `n_assets=1`？
  - 当前结论：禁用（`n_assets=0` 走 `None`），避免冗余参数。多资产再启用。
  - 验证方法：参数量对比 + 跨资产联合训练时的消融。

- 问题：位置编码默认 learned 还是 sin/cos？
  - 当前结论：默认 `learned`，与 BERT 一致；`sincos` 仅作消融与长序列外推备选。
  - 验证方法：固定其它项，对比 `position_type=learned/sincos` 的下游 IC。

- 问题：value 投影用 Linear 还是 MLP？
  - 当前假设：连续特征 ≤ 10 维时 `linear` 足够；扩展到指标后切换 `mlp`。
  - 验证方法：`value_proj` 消融，关注训练曲线与验证集 IC。

- 问题：z-score 窗口长度选 60 是否合理？
  - 当前假设：60 根 K 线（约 2.5 个交易日 @60m）兼顾响应速度与稳定性。
  - 验证方法：`zscore_window ∈ {30, 60, 120}` 三档消融。

- 问题：`dropout=0.1` 在小样本（<5k 序列）上是否过弱？
  - 当前假设：先沿用 BERT 默认；若验证集明显过拟合则升到 0.2–0.3。
  - 验证方法：监控 train/valid loss 间距。
