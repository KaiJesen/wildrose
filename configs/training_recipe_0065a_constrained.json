{
  "experiment": "0065a_leg_align_constrained",
  "init_checkpoint": "prod/v0.0.0/checkpoint/market_state_best.pt",
  "labels_dir": "data/labels/leg_participation",
  "constraint_profile": "constrained",
  "train_defaults": {
    "stride": 1,
    "positive_oversample": 30,
    "participation_weight": 1.5,
    "epochs": 15,
    "early_stop_patience": 6,
    "batch_size": 32,
    "auto_baseline_ic": true,
    "cum_ic_min_ratio": 0.95,
    "early_stop_metric": "composite"
  },
  "variants": {
    "0": {
      "description": "participation only, legacy heads frozen",
      "checkpoint_dir": "checkpoints/0065a_leg_align_c0",
      "report_dir": "reports/0065a_leg_align_c0"
    },
    "1": {
      "description": "c0 + hz 12/24",
      "checkpoint_dir": "checkpoints/0065a_leg_align_c1",
      "report_dir": "reports/0065a_leg_align_c1",
      "init_from": "checkpoints/0065a_leg_align_c0/market_state_best.pt"
    }
  },
  "gates": {
    "participation_auc_valid_min": 0.55,
    "cum_return_ic_degradation_max": 0.05,
    "a1_backtest_return_max_drop": 0.02
  }
}
