# 025 项目进度纪要

| 项 | 内容 |
|----|------|
| 流水号 | 025 |
| 主题 | 多通道参与信号规则扩展（Scheme A3） |
| 纪要日期 | 2026-06-25 |
| 分支 | `feature/024-model-alignment-proposal`（025 工程暂挂此分支，待 Phase 0 通过后可视情况拆 `feature/025-*`） |
| 状态 | **Phase 0 阻塞 — 待决策；明日原机续推** |

---

## 阶段完成情况

| 阶段 | 状态 | 摘要 |
|------|------|------|
| 立项 v1.0 → v1.1 | ✅ | 响应架构师初审 B1~B4、H1~H6 |
| 立项复审 | ✅ | `架构师-025-...立项复审报告.md` — 通过，可进 Phase 0 |
| P0 工程交付 | ✅ | `channel_edge.py`、慢涨参与门、`edge_source` 日志、A3a config、Phase 0/1 编排脚本 |
| Phase 0 B0 复现 | ❌ **FAIL** | 本地重训 checkpoint 无法对齐 024 冻结指标 |
| Phase 1 A3a | ⏸ | 硬门禁未过，**未启动** |
| Phase 2 A3b / A3c | ⏸ | 闸门未解锁 |
| valid 通道调参 | ⏸ | `tune_v025_channel_weights.py` 未实现 |

---

## 当前阻塞问题（必读）

### 1. Phase 0 B0 复现未通过（硬门禁）

| 指标 | 024 报告冻结 B0 | 本机本地重训复现 | 容差 | 状态 |
|------|-----------------|------------------|------|------|
| test `total_return` | **9.01%** | 7.52% | ±0.20pp | FAIL |
| `leg_count_coverage_ratio` | **26.7%** | 31.03% | ±1.0pp | FAIL |
| `teq_open` | **3** | 4 | 精确 | FAIL |

报告：`backtest/v025_phase0/REPORT_025_PHASE0.md`  
摘要：`backtest/v025_phase0/phase0_summary.json`

**根因（按优先级）**：

1. **024 冻结模型权重未入库**：`checkpoints/0065a_leg_align_c1_pw20/market_state_best.pt` 在 `.gitignore` 的 `checkpoints/` 下，GitHub 上不存在；024 报告中的 B0 数字依赖该文件。
2. **024 冻结 TEQ 校准文件缺失**：报告引用 `backtest/v024_constrained/teq_edge_calibration.json`（024 跑数版本）；本机重训后生成了**另一份**同名文件（`long_shift≈0.239`），与 024 原始校准不一定一致。
3. **本机按 recipe 重训 c0→c1_pw20**：`reports/0065a_leg_align_c1_pw20/metrics.json` 已更新，但 valid `part_auc`、早停 epoch 与 024 记录可能不同；叠加训练随机性。
4. **K 线缓存截止日**：本机 `data/cache/..._end20260625.csv`；若原机缓存截止日不同，test 切片与指标会偏移。
5. **立项硬门禁**：Phase 0 要求与 `REPORT_024_CONSTRAINED_FINAL.md` 点对点复现，**不得**在未裁定的情况下用重训权重替代冻结 B0。

### 2. 待项目经理 / 架构师决策（三选一）

| 选项 | 说明 | 后续动作 |
|------|------|----------|
| **A（推荐）** | 从**原机**拷贝 024 冻结 `c1_pw20` checkpoint + 024 版 `teq_edge_calibration.json` | 放入约定路径后 `run_v025_phase0.py --skip-train` |
| **B** | 接受本机重训为新 B0（7.52% / 31.0% / teq=4） | 修订 Phase 0 基准 + 书面记录与 024 偏差 |
| **C** | 放宽 Phase 0 容差（仅方向一致） | 需架构师 + 项目经理书面裁定 |

**在未决策前**：按立项 §4.6，**不得启动 A3a 正式实验**（`run_v025_ab_phase1c.py` 会检查 `phase0_pass`）。

### 3. 原机续推注意事项

- **Git remote**：若 HTTPS `git pull` 挂起，请使用 SSH：`git@github.com:KaiJesen/wildrose.git`（本环境已验证 SSH 可用）。
- **checkpoint 位置**：优先检查原机是否仍有 `checkpoints/0065a_leg_align_c1_pw20/` 及 `backtest/v024_constrained/a2_pw20_wp0.35_test/`（024 最佳回测目录）。
- **虚拟环境**：`setup_env.sh` 为本机草稿，**未入库**；原机沿用既有 `.venv` 即可。
- **首条命令**：`git pull` → `python examples/run_v025_phase0.py --skip-train`（有冻结权重时）或先决策 A/B/C。

---

## 缺失文档与交付物清单

### 已有（本提交入库）

| 路径 | 说明 |
|------|------|
| `软件设计师-025-...立项方案.md` | v1.1 |
| `架构师-025-...立项评审报告.md` | v1.0 |
| `架构师-025-...立项复审报告.md` | 复审通过 |
| `项目进度纪要.md` | 本文件 |
| `trading_system/channel_edge.py` | 通道 edge 映射 |
| `configs/trading_rule_v025_a3a_slow_up.json` | A3a 实验配置 |
| `examples/run_v025_phase0.py` | B0 复现门 |
| `examples/run_v025_ab_phase1c.py` | B0 vs A3a（需 Phase 0 PASS） |
| `backtest/v025_phase0/REPORT_025_PHASE0.md` | Phase 0 报告（FAIL） |

### 缺失 — 024 冻结产物（阻塞 Phase 0）

| 路径 | 说明 | 备注 |
|------|------|------|
| `checkpoints/0065a_leg_align_c1_pw20/market_state_best.pt` | 024 B0 模型 | `.gitignore`，需原机拷贝或 LFS |
| `backtest/v024_constrained/teq_edge_calibration.json` | **024 原版** TEQ 校准 | 本机有一份重训版，**不能等同** |
| `backtest/v024_constrained/a2_pw20_wp0.35_test/` | 024 最佳回测产物 | 用于交叉验证 return/coverage/teq |

### 缺失 — 025 Phase 1+ 计划交付（未阻塞立项，Phase 0 通过后实施）

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `examples/tune_v025_channel_weights.py` | valid 上网格 α,β,γ,τ | P1 |
| `examples/calibrate_channel_edge.py` | 慢涨通道 `ChannelEdgeCalibrator` 拟合 | P1 |
| `backtest/v025/channel_edge_slow_up_calibration.json` | 慢涨通道校准（config 已引用路径） | P1 |
| `backtest/v025_ab/REPORT_025_AB.md` | A3a 一次性 test 验收报告 | P1（需 Phase 0 PASS） |
| `configs/trading_rule_v025_a3b_std_trend.json` | A3b 标准趋势混合 | P2（A3a 裁定后） |
| `configs/trading_rule_v025_a3c_floor.json` | A3c Floor 实验 | P2 |
| `软件设计师-025-评审意见响应与修订说明.md` | 可选；022 有同类文档，025 响应已并入立项 v1.1 §10 | 低 |

### 缺失 — 复审待补项（不阻塞立项，Phase 1 前建议闭合）

| 项 | 说明 | 状态 |
|----|------|------|
| **C1** `slow_up_incremental_coverage_pp` 公式 | 复审报告 §6：相对 B0 的 leg 增量覆盖，公式待代码冻结 | 已在 `run_v025_ab_phase1c.py` 用 coverage 差分，待写入立项附录 |
| **C2** OR 门语义文档化 | `τ_slow` OR `slow_up_edge` 的 valid 选取策略 | 待 `tune_v025` |
| **C3** TEQ `wp` 与通道 `τ_slow` 独立性声明 | 实现已分离，需进 `REPORT_025_AB` 模板 | 待 Phase 1 |
| `edge_source` 字段对照表 | 复审 B2；`REPORT_025_PHASE0.md` 有简表，`eval_participation.py` 未扩展分列 PnL | 部分完成 |

---

## 已交付代码改造（P0）

| 路径 | 改造 |
|------|------|
| `trading_system/config.py` | `ParticipationChannelConfig`、`SlowUpParticipationGateConfig` |
| `trading_system/signal.py` | `slow_up_edge_*`、`effective_edge_*_diag` |
| `trading_system/adapters/market_state_model.py` | `_attach_channel_edges()` |
| `trading_system/rules.py` | 慢涨参与门、`edge_source` diagnostics |
| `trading_system/logger.py` | `edge_source`、通道 edge 列 |
| `trading_system/sizing.py` | `probe_ratio` 缩放慢涨首仓 |

---

## 明日原机续推建议顺序

```bash
# 1. 拉取本提交
git pull   # 若 HTTPS 失败：git remote set-url origin git@github.com:KaiJesen/wildrose.git

# 2. 确认/恢复 024 冻结产物（选项 A）
ls checkpoints/0065a_leg_align_c1_pw20/market_state_best.pt
ls backtest/v024_constrained/teq_edge_calibration.json

# 3. Phase 0 复现门
python examples/run_v025_phase0.py --skip-train
# 期望：phase0_summary.json → "phase0_pass": true

# 4. Phase 1 A3a（仅 Phase 0 PASS 后）
python examples/run_v025_ab_phase1c.py

# 5. 若 A3a 探索线未闭合 → valid 调参（待实现 tune_v025）
# python examples/tune_v025_channel_weights.py
```

**若 Phase 0 仍 FAIL**：先完成上文「待决策」A/B/C，**不要**强行跑 A3a。

---

## 相关参考

- 024 结案：`document/024_趋势腿对齐模型信号修正/项目进度纪要.md`
- 024 最终调参：`backtest/v024_constrained/REPORT_024_CONSTRAINED_FINAL.md`
- 024 训练记录：`document/024_.../训练师-024-0065a约束训练与TEQ调优记录.md`
- 025 立项 v1.1：`软件设计师-025-多通道参与信号规则扩展立项方案.md`

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-25 | 初版：Phase 0 FAIL、缺失清单、明日续推指引 |
