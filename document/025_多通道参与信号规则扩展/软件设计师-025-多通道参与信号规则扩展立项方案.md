# 多通道参与信号规则扩展立项方案

## 1. 文档说明

| 项 | 内容 |
|----|------|
| 角色 | 软件设计师 |
| 流水号 | 025 |
| 主题 | 在冻结模型 checkpoint 上，将 024 腿对齐参与分数 **受控扩展** 至 TEQ 以外开仓通道，闭合探索线 coverage 缺口 |
| 版本 | v1.0 |
| 依据 | `document/024_趋势腿对齐模型信号修正/项目进度纪要.md`、`训练师-024-0065a约束训练与TEQ调优记录.md`、`backtest/v024_constrained/REPORT_024_CONSTRAINED_FINAL.md` |
| 前置冻结 | `configs/trading_rule_v023_phase1c_0062e.json`（规则主体）、`checkpoints/0065a_leg_align_c1_pw20`（模型）、`configs/trading_rule_v024_phase1c_teq_0065a_c1_pw20.json`（024 TEQ 最佳） |
| 024 裁定 | Scheme B（仅 TEQ）已达 Pareto 膝点；**选项 D** — 规则层 / Scheme A3 受控实验 |
| 目标读者 | 程序员、回测师、架构师、项目经理 |

**立项动因（一句话）**：

```text
024 在 TEQ 单通道上已实现 return 9.01%（超探索线），但 coverage 26.7%（差 28% 目标 1.3pp）；
继续调 w_part 会在 wp≈0.37 处牺牲 0.46pp 收益换 3.3pp 覆盖率——需从规则消费面扩展，而非再训模型。
```

**架构约束（继承 024 §6.4.2）**：

```text
禁止全局 effective_edge 覆盖 TradingSignal.edge
标准 OPEN_* / reverse / continue 的 legacy edge 语义不变
A3 扩展仅限显式白名单通道 + 诊断字段 + A/B 归因
Phase 4 prod 提名不得依赖 A3，除非探索线/投产线均 PASS 且项目经理书面批准
```

---

## 2. 问题归纳

### 2.1 024 已证明什么

| 结论 | 证据 |
|------|------|
| 参与信号可学 | valid part_auc ≈ 0.57（constrained） |
| TEQ 接线有效 | teq 3×，counter_leg ≤5 |
| 收益可超 A0 | A2 return **9.01%** vs A0 **7.77%** |
| 单通道 coverage 有上限 | wp≥0.37 时 return 跌至 8.55% |
| 慢涨通道未参与 | phase1c `slow_up_open_count=0`，`watch_slow_uptrend` 大量 watch 无仓 |

### 2.2 未覆盖的参与盲区（按 bar 量级）

| 通道 | phase1c 表现 | 024 Scheme B | 025 目标 |
|------|-------------|--------------|----------|
| TrendEntryQualifier（teq） | teq≈1 | teq=3，已调优 | 保持 024 最佳 wp=0.35 |
| 慢涨 `OPEN_LONG_SLOW_TREND` | **0 开仓** | 不读 `teq_edge_*` | **P0：受控启用 + 参与门** |
| 标准趋势 `OPEN_*_SIGNAL` | edge 门槛高、空仓多 | 仍读 legacy `edge` | **P1：confirmed leg 子集条件混合** |
| Participation Floor（023 P2） | 默认关闭 | 未实验 | **P2：可选对照** |

### 2.3 核心假设

coverage 缺口主要来自 **「趋势已确认、规则允许、但模型 legacy edge 未过线」** 的 bar，而非 TEQ 阈值 alone。将 **已校准的 `participate_score_*` / `teq_edge_*`** 在 **白名单通道** 上作为 **附加资格或阈值放松因子**，有机会增加有效 overlap 而不复制 teq ceiling 的噪声开仓模式。

---

## 3. 目标与范围

### 3.1 总目标（分层验收，继承 024 §5.1）

在 **冻结 0065a-c1_pw20 checkpoint** 与 **024 TEQ 最佳权重** 上，仅改规则消费逻辑：

| 阶段 | 最低要求 |
|------|----------|
| Phase 0 | 复现 024 A2 best（return 9.01% ±0.2pp，coverage 26.7% ±1pp） |
| Phase 1 | 慢涨通道 A3a：coverage ≥ **28%** 且 return ≥ **8.84%** |
| Phase 2 | 标准趋势 A3b（若 Phase 1 未闭合）：同上探索线 |
| Phase 3 | 合并最佳 A3* + multi-seed 稳定性；**探索线双门 PASS** |
| Phase 4 | 投产线（return ≥9.31%，coverage ≥35%）— 仅探索 PASS 后评估 |

辅助监控（非单点否决除非注明）：

- `counter_leg_participation_count` ≤ 024 baseline + 2
- `slow_up_false_entry_count` ≤ 3（test）
- `max_drawdown` ≥ −2.0%（test）
- `trend_qualified_pnl` ≥ 0

### 3.2 范围内

- `trading_system/rules.py`：白名单通道读取 `participate_score_*` / 通道专用 `effective_edge_*`
- `trading_system/signal.py`：诊断字段 `effective_edge_long_diag` / `effective_edge_short_diag`（**不写回** `edge`）
- `trading_system/config.py`：`ParticipationChannelConfig`（每通道开关、权重、门槛）
- `configs/trading_rule_v025_*.json`：A3 实验配置族
- `examples/run_v025_ab_phase1c.py`：A0/A2/B0/A3a/A3b 矩阵
- `examples/eval_participation.py`：沿用 023 §5.3 口径

### 3.3 范围外

- **不重训** 0065a / 不替换 c1_pw20 checkpoint（除非 Phase 3 全失败且架构师另批）
- 不改 `TrendSegment` / `TrendSignal` / `TrendBias` 核心逻辑
- 不改 RiskBudget 硬前置
- 不全局替换 `TradingSignal.edge`
- 不在默认路径启用 Participation Floor（仅 A3c 实验臂）
- 不启动 017 best-point 融合（留待 024 §6.6 条件，本版不触发）

---

## 4. 方案设计（Scheme A3 受控扩展）

### 4.1 信号契约（增量）

在 024 `TradingSignal` 已有字段基础上：

```python
# 诊断（全 bar 可记录，不参与未声明通道）
effective_edge_long_diag: float = 0.0
effective_edge_short_diag: float = 0.0

# 通道专用（仅声明通道读取）
slow_up_edge_long: float = 0.0      # 慢涨多头资格
std_trend_edge_long: float = 0.0    # 标准趋势多头（可选）
std_trend_edge_short: float = 0.0   # 标准趋势空头（可选）
```

**计算原则**（valid 调参，禁止 test 泄漏）：

```text
effective_edge_long_diag = α·legacy_edge + β·teq_edge_long + γ·(2·participate_score_long - 1)
慢涨/标准通道仅读取对应 *_edge_* 字段，禁止写回 signal.edge
```

默认初值建议：`α=0.6, β=0.25, γ=0.15`（慢涨偏重 legacy 防误开）；各通道独立 valid 网格。

### 4.2 通道 A3a — 慢涨开仓（P0）

**背景**：023 phase1c `slow_up_position.enabled=false`；024 未改。test 上 `watch_slow_uptrend` 多但 `slow_up_open_count=0`，是 coverage 最大潜在来源。

**规则增量**（`RuleEngine._can_open_slow_up` / `_try_open_slow_up`）：

1. 配置 `slow_up_position.enabled=true`（025 实验 config）
2. 新增 `slow_up_participation_gate`：
   - `participate_score_long ≥ τ_slow` **或** `slow_up_edge_long ≥ edge_threshold_slow`
   - `τ_slow` 初值 0.55；valid 网格 [0.45, 0.65]
3. 保留 023 既有：`is_stable_slow_uptrend`、RiskBudget、`min_hold_bars`
4. 首仓 probe 比例 ≤ `slow_up_probe_ratio`（默认 0.5× 正常趋势仓）

**归因**：`metrics.slow_up_*` 分列 PnL；`eval_participation.slow_up_false_entry_count` 必报。

### 4.3 通道 A3b — 标准趋势 confirmed-leg 条件混合（P1）

**背景**：大量 confirmed leg bar 上 legacy `edge` 不够，但 `teq_edge` 或 `participate_score` 已指示参与。

**规则增量**（仅当 **全部** 条件满足时替换比较对象）：

```text
is_leg_confirmed == 1
leg_type ∈ {FAST_UP_LEG, SLOW_UP_LEG, FAST_DOWN_LEG, SLOW_DOWN_LEG}
NOT chop_hard
portfolio_flat
→ 开仓阈值比较改用 std_trend_edge_*（非 signal.edge）
```

**安全阀**：

- 仅影响 `OPEN_LONG_TREND` / `OPEN_SHORT_TREND` 类 reason，不影响 crash / sentinel
- `std_trend_edge` 混合权重 `β` ≤ teq 通道（防止标准通道过度放松）
- valid 上网格；若 A3a 已闭合探索线，A3b 降为 P2 对照

### 4.4 通道 A3c — Participation Floor（P2，可选）

继承 023 §6.6.2 设计，默认 **关闭**。仅当 A3a+A3b 仍不达 28% coverage 时启用实验臂：

- confirmed leg + flat + `participate_score ≥ τ_floor` → 允许最小 probe 开仓
- 不得绕过 RiskBudget；`τ_floor` valid 选取

### 4.5 与 024 Scheme B 的关系

```text
B0 = 024 最佳（仅 TEQ 读 teq_edge，wp=0.35）— 所有 A3 实验的对照基线
A3a = B0 + 慢涨通道参与门
A3b = A3a + 标准趋势条件混合（或 B0 + A3b 消融）
A3c = A3b + Floor（可选）
```

**硬约束**：TEQ 路径继续读 `teq_edge_*`，**不**改为读 `effective_edge_diag`。

---

## 5. 验收与 A/B 矩阵

### 5.1 实验臂

| 臂 | 模型 | 规则 |
|----|------|------|
| A0 | 0062e | phase1c |
| A2 | c1_pw20 | 024 TEQ best（B0） |
| A3a | c1_pw20 | B0 + 慢涨参与门 |
| A3b | c1_pw20 | A3a + 标准趋势混合 |
| A3c | c1_pw20 | A3b + Floor（可选） |

### 5.2 探索线（test，一次性）

| 指标 | 门槛 |
|------|------|
| `total_return` | ≥ **8.84%** |
| `leg_count_coverage_ratio` | ≥ **28%** |
| `counter_leg_participation_count` | ≤ B0 + 2 |

### 5.3 Phase 0 复现门

| 指标 | B0 基准 | 容差 |
|------|---------|------|
| return | 9.01% | ±0.20pp |
| coverage | 26.7% | ±1.0pp |
| teq opens | 3 | 精确 |

未过复现门不得进入 A3 实验。

---

## 6. 代码改造清单

| 优先级 | 路径 | 改造 |
|--------|------|------|
| P0 | `trading_system/config.py` | `ParticipationChannelConfig`、`SlowUpParticipationGate` |
| P0 | `trading_system/signal.py` | `effective_edge_*_diag`、`slow_up_edge_*` |
| P0 | `trading_system/adapters/market_state_model.py` | 通道 edge 映射（不改 legacy edge） |
| P0 | `trading_system/rules.py` | 慢涨门 + 诊断日志 reason 子码 |
| P0 | `configs/trading_rule_v025_a3a_slow_up.json` | A3a 实验配置 |
| P0 | `examples/run_v025_ab_phase1c.py` | A/B 编排 + `REPORT_025_AB.md` |
| P1 | `trading_system/rules.py` | A3b 标准趋势条件混合 |
| P1 | `configs/trading_rule_v025_a3b_std_trend.json` | A3b 配置 |
| P1 | `examples/tune_v025_channel_weights.py` | valid 上网格（α,β,γ,τ） |
| P2 | `configs/trading_rule_v025_a3c_floor.json` | Floor 实验 |
| P2 | `document/025_.../项目进度纪要.md` | 阶段裁定 |

---

## 7. 里程碑

| 阶段 | 时间 | 交付物 | 出口条件 |
|------|------|--------|----------|
| Phase 0 | W1 | B0 复现报告 | 与 024 最佳一致（容差内） |
| Phase 1 | W1~W2 | A3a 慢涨 + valid 调参 | 探索线 PASS **或** 明确慢涨无效假设 |
| Phase 2 | W2~W3 | A3b 标准混合 | 探索线 PASS **或** 进入 A3c / 驳回 |
| Phase 3 | W3~W4 | multi-seed（≥3）+ `REPORT_025_AB.md` | ≥2 seed 探索 return；≥1 seed 探索双门 |
| Phase 4 | W4 | prod 提名评估 | 投产线 PASS（否则维持 0062e） |

---

## 8. 风险与缓解

| 风险 | 缓解 |
|------|------|
| 误伤标准开仓、收益崩塌 | 白名单 + confirmed leg + 不调 global edge |
| 慢涨假突破 | `τ_slow` valid 网格 + `slow_up_false_entry_count` 上限 |
| 与 TEQ 双重计数 | 通道归因 PnL 分列；decisions.csv 记录 `edge_source` |
| test 调参泄漏 | 通道权重仅 valid 选取；test 一次性验收 |
| A3 依赖未审计 | Phase 4 默认禁止 prod；须双轨 PASS + 书面批准 |

---

## 9. 失败与回退

| 条件 | 动作 |
|------|------|
| Phase 1 慢涨无效（coverage +<1pp 或 return −>2pp） | 关闭 A3a，仅试 A3b |
| Phase 2 仍不达 28% coverage | 试 A3c Floor 或驳回 A3 路径 |
| 探索线仍近失 | 回退 B0；025 结案；评估 024 §6.6 best-point 或标签 v2 |
| 任意阶段 drift 恶化 >2pp vs A0 | 回滚该通道增量 |

**回退基线**：`configs/trading_rule_v024_phase1c_teq_0065a_c1_pw20.json` + `checkpoints/0065a_leg_align_c1_pw20`。

---

## 10. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-25 | 024 Phase 3 近失后选项 D 立项；Scheme A3 受控多通道扩展 |
